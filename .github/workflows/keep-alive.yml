name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes, exactly
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -s https://saqibmazhar.onrender.com/api/projects > /dev/null || true
